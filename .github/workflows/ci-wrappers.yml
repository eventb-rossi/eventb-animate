name: CI Wrappers

# The wrappers download a released jar rather than building one, so nothing in
# src/ can affect them — and a Java-only pull request should not pull ~94 MiB.
on:
  push:
    branches:
      - main
    paths:
      - 'action.yml'
      - 'setup/action.yml'
      - '.gitlab-ci-template.yml'
      - 'scripts/install-eventb-animate.sh'
      - 'scripts/extract-ci-shell.rb'
      - '.github/workflows/ci-wrappers.yml'
  pull_request:
    # Repeated because workflows do not support YAML anchors.
    paths:
      - 'action.yml'
      - 'setup/action.yml'
      - '.gitlab-ci-template.yml'
      - 'scripts/install-eventb-animate.sh'
      - 'scripts/extract-ci-shell.rb'
      - '.github/workflows/ci-wrappers.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      # The actions and GitLab wrapper keep shell inside YAML, where the
      # scripts glob cannot reach it.
      - run: ruby scripts/extract-ci-shell.rb "$RUNNER_TEMP/ci-shell"
      - run: shellcheck scripts/*.sh "$RUNNER_TEMP"/ci-shell/*.sh

  # The download path: tag resolution, checksum verification, and the refusal to
  # install what the manifest does not vouch for. macOS is here because it has
  # no `sha256sum`; the fallback only runs there.
  setup-action:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            java-version: ''
            preinstall-java: true
          - os: macos-latest
            java-version: '21'
            preinstall-java: false
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    # Every matrix leg resolves the same tag, so the racing writes agree.
    outputs:
      version: ${{ steps.install.outputs.version }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        if: ${{ matrix.preinstall-java }}
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install the latest release
        id: install
        uses: ./setup
        with:
          java-version: ${{ matrix.java-version }}

      - name: The PATH launcher and outputs name the installed release
        shell: bash
        env:
          JAR_PATH: ${{ steps.install.outputs.jar-path }}
          RESOLVED: ${{ steps.install.outputs.version }}
        run: |
          set -euo pipefail
          test -s "$JAR_PATH"
          java -jar "$JAR_PATH" --version | grep -Fxq "eventb-animate ${RESOLVED#v}"
          eventb-animate --version | grep -Fxq "eventb-animate ${RESOLVED#v}"

      # v6.1 is a real release published before SHA256SUMS existed; v99.0 does
      # not exist. Both are refused for the same stated reason, and neither
      # costs a jar download — the manifest is fetched first.
      - name: A release the manifest cannot vouch for is refused
        shell: bash
        run: |
          set -euo pipefail
          for tag in v6.1 v99.0; do
            if output=$(EVENTB_ANIMATE_VERSION="$tag" bash scripts/install-eventb-animate.sh 2>&1); then
              echo "installing $tag must fail"
              exit 1
            fi
            printf '%s\n' "$output"
            # Not merely a non-zero exit: the refusal has to be the manifest.
            grep -Fq 'with a SHA256SUMS manifest' <<< "$output"
            test -z "$(find "$RUNNER_TEMP/eventb-animate-$tag" -name '*.jar' 2>/dev/null)"
          done

      - name: A malformed version cannot inject runner commands
        shell: bash
        run: |
          set -euo pipefail
          version=$'v6.4\n::warning::forged'
          if output=$(EVENTB_ANIMATE_VERSION="$version" bash scripts/install-eventb-animate.sh 2>&1); then
            echo "installing a malformed version must fail"
            exit 1
          fi
          grep -Fq "version must be 'latest' or a release tag" <<< "$output"
          ! grep -Fq '::warning::forged' <<< "$output"

      # The first install left a verified jar in place; a rerun must reuse it,
      # and a corrupted one — the shape of a poisoned cache restore — must be
      # replaced rather than trusted. The reuse run keeps `latest` on purpose:
      # the actions now pass a resolved tag down, so only a direct run covers
      # the installer's own resolution branch. Exiting zero after the
      # corruption means the replacement passed the checksum check.
      - name: A cached jar is verified before reuse; a corrupted one is replaced
        shell: bash
        env:
          RESOLVED: ${{ steps.install.outputs.version }}
          JAR_PATH: ${{ steps.install.outputs.jar-path }}
        run: |
          set -euo pipefail
          output=$(bash scripts/install-eventb-animate.sh 2>&1)
          printf '%s\n' "$output"
          grep -Fq 'skipping download' <<< "$output"
          echo corrupted > "$JAR_PATH"
          output=$(EVENTB_ANIMATE_VERSION="$RESOLVED" bash scripts/install-eventb-animate.sh 2>&1)
          printf '%s\n' "$output"
          ! grep -Fq 'skipping download' <<< "$output"

  # The only in-repo proof that the key the actions save under is the key they
  # look up: the setup-action job's post-job cache save has completed by the
  # time a dependent job starts.
  cache-roundtrip:
    needs: setup-action
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          sparse-checkout: scripts
      # Pinned to what setup-action installed: resolving is then offline, and
      # a release published mid-run cannot skew the key under lookup.
      - name: Resolve the release tag
        id: resolve
        shell: bash
        env:
          EVENTB_ANIMATE_VERSION: ${{ needs.setup-action.outputs.version }}
          EVENTB_ANIMATE_RESOLVE_ONLY: 'true'
        run: bash scripts/install-eventb-animate.sh
      - name: The saved cache is found under the resolved tag
        uses: actions/cache/restore@v4
        with:
          path: ${{ steps.resolve.outputs.jar-path }}
          key: ${{ steps.resolve.outputs.cache-key }}
          lookup-only: true
          fail-on-cache-miss: true

  # End to end, which is what catches a mis-wired jar path between the two
  # steps. Bounded to one state: the point is the wrapper, not the check.
  action:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      # A missing condition would fail inside setup-java before the installer
      # creates its release directory. v6.1 then supplies the expected cheap
      # failure: it predates the checksum manifest, so no jar is downloaded.
      - name: Empty Java version skips setup-java
        id: skip-java
        continue-on-error: true
        uses: ./
        with:
          java-version: ''
          version: v6.1
          # Nothing is ever saved under a release that always fails to
          # install, so the restore and post-job save would only add noise.
          cache: 'false'
          model-path: src/test/resources/models/traffic-light/M2.bum

      - name: The skipped Java setup reached the installer
        shell: bash
        env:
          OUTCOME: ${{ steps.skip-java.outcome }}
        run: |
          set -euo pipefail
          test "$OUTCOME" = failure
          test -d "$RUNNER_TEMP/eventb-animate-v6.1"

      - uses: ./
        with:
          model-path: src/test/resources/models/traffic-light/M2.bum
          states: 1
          json-report: report.json
      - name: The run produced a report
        shell: bash
        run: |
          set -euo pipefail
          test -s report.json
          jq -e '.completion.reason == "state_limit"' report.json
          test -z "$(find "$RUNNER_TEMP" -type f -name eventb-animate 2>/dev/null)"

  # The same end-to-end run for the other wrapper, which repeats the installer
  # inline and would otherwise drift unnoticed. Both of its script blocks run in
  # one shell, as GitLab runs them, in the image the template itself declares.
  gitlab-template:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - name: Extract the template's shell
        run: ruby scripts/extract-ci-shell.rb ci-shell

      # --env-file carries the template's own `variables:` defaults, the way
      # GitLab injects them; a later --env overrides one of them.
      - name: A model is checked with a verified jar
        run: |
          set -euo pipefail
          docker run --rm --volume "$PWD:/work" --workdir /work \
            --env-file ci-shell/gitlab-variables.env --env CI_JOB_ID=test \
            --env EVENTB_ANIMATE_MODEL_PATH=src/test/resources/models/traffic-light/M2.bum \
            --env EVENTB_ANIMATE_STATES=1 \
            "$(cat ci-shell/gitlab-image)" bash ci-shell/gitlab-script.sh

      - name: A release the manifest cannot vouch for is refused
        run: |
          set -euo pipefail
          if output=$(docker run --rm --volume "$PWD:/work" --workdir /work \
            --env-file ci-shell/gitlab-variables.env --env CI_JOB_ID=test \
            --env EVENTB_ANIMATE_VERSION=v6.1 \
            --env EVENTB_ANIMATE_MODEL_PATH=src/test/resources/models/traffic-light/M2.bum \
            "$(cat ci-shell/gitlab-image)" bash ci-shell/gitlab-script.sh 2>&1); then
            echo "installing an unverifiable release must fail"
            exit 1
          fi
          printf '%s\n' "$output"
          grep -Fq 'with a SHA256SUMS manifest' <<< "$output"
